namespace ApplicationAccountingSystem.Domain.Designation
{
    public enum TicketStatus
    {
        New = 0,
        InProgress = 1,
        waitingForCustomer = 2,
        Resolved = 3,
        Closed = 4
    }
}